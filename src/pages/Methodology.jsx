import React from 'react'
import { Link } from 'react-router-dom'

// Static methodology / "how it's computed" reference. Mirrors the served implementation
// (Dixon-Coles strength -> score matrix -> calibration -> de-vig market -> EV/Kelly). Keep in
// sync with src/worldcup/{strength_model,poisson,calibration,betting,clv}.py if the model changes.

function S({ id, title, children }) {
  return <section className="mdsec" id={id}><h2>{title}</h2>{children}</section>
}
function F({ children }) { return <div className="mdformula">{children}</div> }

export default function Methodology() {
  return (
    <div className="wrap methodology">
      <header>
        <h1>预测方法与算法说明</h1>
        <div className="sub"><Link to="/" className="navlink">← 返回首页</Link> · 模型/市场/混合/EV 的具体算法与细节</div>
      </header>

      <div className="note">
        本页说明网页上每个数字“怎么算出来的”。核心是一条标准的现代足球预测链：
        历史拟合实力评级 → 单场进球率 λ → 比分概率矩阵 → 校准 → 1X2/比分/大小球/让球；
        市场侧做去抽水得到“无水概率”，再与模型对比算价值(EV)与凯利仓位。当前为<b>影子模式</b>，只记录不下注。
      </div>

      <nav className="mdtoc">
        {[['data', '1. 数据来源'], ['strength', '2. 实力评级 (Dixon-Coles)'], ['match', '3. 单场预测 λ→比分矩阵'],
        ['calib', '4. 校准'], ['market', '5. 市场无水概率'], ['blend', '6. 模型 vs 市场 / 混合'],
        ['ev', '7. 价值 EV 与凯利仓位'], ['clv', '8. CLV 与影子门槛'], ['acc', '9. 命中率口径'], ['limit', '10. 局限与诚实声明']]
          .map(([k, l]) => <a href={`#${k}`} key={k}>{l}</a>)}
      </nav>

      <S id="data" title="1. 数据来源">
        <ul>
          <li><b>历史国际比赛</b>（约 8000+ 场，含俱乐部/国家队市场赔率与 xG）：用于拟合实力评级，是模型“技能”的来源。</li>
          <li><b>FotMob</b>（免 token 的 SSR 页面）：本届每场的 xG/射门/射门点图/球员数据/首发/裁判，赛后回填；也用于把淘汰赛比分校正为 90 分钟。</li>
          <li><b>The Odds API</b>：开盘/收盘赔率（1X2、让球、大小球，多家书商）与完赛比分。</li>
          <li><b>天气/海拔/教练/风格</b>：仅作展示与球探，<b>不进入</b>模型计算（见第 3 节）。</li>
        </ul>
      </S>

      <S id="strength" title="2. 实力评级 — 拟合的 Dixon-Coles（最大似然）">
        <p>每支球队有一个进攻强度 αᵢ 与防守强度 βᵢ，外加一个全局主场系数 γ。一场比赛主客队的期望进球：</p>
        <F>λ_home = αₕ · β_a · √γ　　λ_away = α_a · βₕ / √γ</F>
        <p>参数由历史结果的<b>加权泊松对数似然</b>最大化得到（不是手调）。两点关键：</p>
        <ul>
          <li><b>时间衰减</b>：每场样本按指数衰减加权，半衰期 <b>730 天</b>（约 2 年）。扫描验证 730d 接近最优、更短反而更差——历史并未“过度锚定”。</li>
          <li><b>低比分相关修正 (rho)</b>：纯泊松会低估 0-0/1-1、高估 1-0/0-1，用 Dixon-Coles 的 τ 修正这四个低分格，rho 由一维搜索拟合（约 −0.05）。</li>
        </ul>
        <p>评级在“赛前历史”上拟合（不含本届当场），因此对本届是<b>样本外</b>的；本届结果每天回灌训练库，让评级跟上状态。走步前向回测（3562 折）：对数损失 ≈ <b>0.852</b>、ECE ≈ 0.04、命中率 ≈ 60.6%——处于国际足球的现实天花板。</p>
      </S>

      <S id="match" title="3. 单场预测 — 从 λ 到比分概率矩阵">
        <p>得到 λ_home、λ_away 后，先做<b>有依据的</b>情境微调（对数空间相加），再生成比分矩阵：</p>
        <ul>
          <li><b>计入</b>：拟合的主场优势（仅真正的东道主主场）、伤病/停赛/首发缺失/疲劳（休整与旅程）penalty——这些有数据支撑。</li>
          <li><b>不计入</b>（2026-06 移除，仅展示）：天气、海拔、教练经验、风格/战术克制。它们都是“未拟合的手调先验”，回测显示对对数损失基本为 0 甚至有害，所以净倍率恒为 1.0、不影响进球。</li>
        </ul>
        <p>比分矩阵 P(i,j)：双泊松 × Dixon-Coles 低分修正 τ：</p>
        <F>P(i,j) = τ(i,j; rho) · Pois(i; λ_home) · Pois(j; λ_away)，最后整体归一化（Σ=1）</F>
        <p>由这张矩阵直接汇总出所有输出：<b>1X2</b>（胜=Σ P(i,j) 其中 i&gt;j；平= i=j；负= i&lt;j）、<b>最可能比分/前12比分</b>、<b>大小球</b>（按总进球阈值求和）、<b>让球</b>（按净胜球阈值求和，含亚盘半输半赢）。淘汰赛同样是 <b>90 分钟</b> 的 1X2（平局是合法结果，不预测加时/点球“谁晋级”）。</p>
      </S>

      <S id="calib" title="4. 校准（让概率不过度自信/保守）">
        <p>用<b>温度缩放</b>把原始概率 p 调成 p^(1/T) 再归一化：T&gt;1 软化（更接近 50/50），T&lt;1 锐化。</p>
        <ul>
          <li>历史样本外拟合出的校准器 <b>T≈1.149</b>（轻微软化），目前<b>仅监控不上线</b>。</li>
          <li>实际服务的是 <b>sharpen α=1.1</b>（即 T≈0.91，轻微锐化，抵消“收藏夹被压平”）。两者方向相反——这是一个需要决策的取舍，已在监控中跟踪 WC 隐含温度的漂移。</li>
        </ul>
      </S>

      <S id="market" title="5. 市场无水概率（去抽水）">
        <p>书商赔率含“水位”（margin），三项隐含概率之和 &gt; 1。去抽水即归一化：</p>
        <F>q_i = (1/odds_i) / Σ_k (1/odds_k)　（对 1X2 的三项；让球/大小球按两项）</F>
        <ul>
          <li>跨多家书商取<b>中位数</b>赔率再去抽水（稳健，避免被单家离群价造出假价值）。</li>
          <li>“最优价”仅用于<b>比价</b>展示，不用于算价值。</li>
        </ul>
      </S>

      <S id="blend" title="6. 模型 vs 市场 / 混合">
        <ul>
          <li><b>仪表盘展示的预测%</b> = 纯校准模型概率（<code>outcome_probabilities</code>），<b>不</b>混合市场。</li>
          <li><b>详情页公布的“综合 1X2”</b> = 0.25×模型 + 0.75×市场（更贴近共识的稳健展示）；原始模型值同时保留。</li>
          <li><b>下注路径</b>：用独立模型，市场只作“背离”参照（h2h 混合权重=1.0=不向市场收缩）；让球/大小球向共识收缩（权重 0.25，因软盘上模型打不过收盘价）。</li>
        </ul>
        <p className="dim small">为什么不同：流动的 1X2 与 Pinnacle 大小球收盘价已包含进球模型知道的一切，向其混合会让模型“贴合”而非“击败”市场；下注侧因此不混合 h2h，只在软盘/背离处看机会。</p>
      </S>

      <S id="ev" title="7. 价值 EV 与凯利仓位">
        <p>对某一选项，模型概率 p、可投赔率 odds（用稳健中位价），价值（期望收益）：</p>
        <F>EV = p·(odds − 1) − (1 − p)　　（亚盘半赢/半输/走盘按比例拆分，避免假 EV）</F>
        <p>仓位用<b>分数凯利</b>：</p>
        <F>f* = EV / (odds − 1) = (p·b − q)/b，实投 = 0.25 × f*（再按模型不确定性收缩）</F>
        <p>风控闸门：单注 ≤ 1% 资金、单日 ≤ 3%、最小 EV 2%、最小边际 1.5%、赔率过期/质量不达标即否决。</p>
      </S>

      <S id="clv" title="8. CLV 与影子晋级门槛">
        <p><b>CLV（收盘价值）</b>衡量“是否打得过收盘价”，用<b>概率口径</b>（两侧都去抽水）：</p>
        <F>CLV_prob = 收盘无水概率 − 开盘无水共识概率（&gt;0 表示开盘价更优 = 打过了收盘）</F>
        <p className="dim small">注意：另一种“小数赔率口径”用“最优开盘价 ÷ 单一书商收盘价”会系统性虚高，已弃用于判定。</p>
        <p>晋级到真实下注需要：在<b>“会下注”样本</b>（只被影子/质量门拦、而非被边际/EV/过期否决的信号）上累计 ≥ <b>300</b> 条且<b>概率 CLV 为正</b>，外加人工批准。当前样本仍不足、且整体概率 CLV ≈ 0，故<b>保持影子、不下注</b>。</p>
      </S>

      <S id="acc" title="9. 首页命中率的口径">
        <p>首页“预测战绩”只用<b>开球前生成</b>的预测快照打分（从带时间戳的预测留痕里取 generated_at &lt; 开球时间的那条），<b>杜绝赛果泄漏</b>；早期无赛前快照的比赛诚实跳过。比分相关均按 <b>90 分钟</b> 赛果判定。指标：胜平负命中、赛果是否落在模型前12比分、正确比分、大小球方向、按置信档的校准。</p>
      </S>

      <S id="limit" title="10. 局限与诚实声明">
        <ul>
          <li>模型校准良好、命中率接近国际足球天花板，但<b>尚未证明能稳定打过市场</b>：诚实口径的概率 CLV ≈ 0，软盘上打不过 Pinnacle 收盘价。</li>
          <li>“价值/EV”多为模型与市场的背离，<b>EV 大小不预测盈利</b>（不要按边际大小加仓）。</li>
          <li>平局被系统性低估（模型≈市场≈21% vs 实际≈26%），但这是模型与市场<b>共有</b>的方差，非模型独有缺陷。</li>
          <li>淘汰赛只预测 90 分钟结果，不预测加时/点球的晋级方。</li>
        </ul>
      </S>

      <footer>世界杯 2026 数据看板 · 方法说明 · 仅供分析，非投注建议</footer>
    </div>
  )
}
